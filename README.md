# Todo-List-Application

A simple and clean Todo List application built using **HTML, CSS, and JavaScript**.  
This project helps users add tasks, track progress, mark tasks as finished, delete tasks, and store everything using **LocalStorage**.

---

## 🚀 Features

- Add new tasks
- Mark tasks as **Open** or **Finished**
- Delete tasks easily
- Search tasks instantly
- Filter tasks:
  - All Tasks
  - Open Tasks
  - Finished Tasks
- Tasks are saved permanently using **LocalStorage**
- Responsive and modern UI

---

## 🛠️ Technologies Used

- HTML5
- CSS3
- JavaScript (DOM Manipulation)
- LocalStorage API
- Font Awesome Icons

---

## ⚙️ How It Works

1. User adds a task using the plus button input.
2. Tasks are displayed dynamically on the page.
3. Tasks can be marked as finished or reopened.
4. Tasks can be deleted anytime.
5. All tasks are stored in browser LocalStorage, so refreshing the page does not remove them.